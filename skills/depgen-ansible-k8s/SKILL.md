---
name: depgen-ansible-k8s
description: >
  Generate a Dockerfile and a DEPLOYMENT.md specification for deploying an application
  using Ansible and Kubernetes. Supports Spring Boot (Java), Laravel (PHP), and Node.js
  application stacks. Auto-detects the stack from project files (pom.xml, composer.json,
  package.json), reads CLAUDE.md dependencies, SPECIFICATION.md tech stack, and the
  application's externalized environment variables to produce a complete deployment blueprint.
  Standardized input: application name (mandatory).
  Use this skill whenever the user asks to create deployment artifacts, Dockerfiles,
  Kubernetes manifests, Ansible playbooks, or deployment documentation for an application.
  Also trigger when the user says things like "deploy this app", "containerize this",
  "create a Dockerfile", "write deployment docs", "generate k8s manifests", or any request
  for deployment-related artifacts.
---

# Deployment Artifact Generator — Ansible & Kubernetes

This skill generates two deployment artifacts for an application:

1. **Dockerfile** — A production-ready, multi-stage container build file placed at the
   application root.
2. **DEPLOYMENT.md** — A comprehensive deployment specification document that serves as a
   blueprint for both AI coding agents and human DevOps engineers to deploy the application
   using Ansible and Kubernetes.

The skill does NOT create the actual Ansible playbooks or Kubernetes manifest files. It
produces a detailed specification document containing complete, copy-pasteable YAML samples
for every manifest and playbook, so that implementation becomes a mechanical exercise.

## Inputs

```
/depgen-ansible-k8s <application>
```

| Argument | Required | Example | Description |
|----------|----------|---------|-------------|
| `<application>` | Yes | `hub_middleware` | Application name to locate the source code and context folder |

No `version:` or `module:` arguments — deployment is application-level.

### Input Resolution

The application name is matched against root-level application folders:
1. Strip any leading `<number>_` prefix from folder names (e.g., `1_hub_middleware` → `hub_middleware`)
2. Match case-insensitively against the provided application name
3. Accept snake_case, kebab-case, or title-case input
4. If no match found, list available applications and stop

### Auto-Resolved Paths

| File | Resolved Path |
|------|---------------|
| CLAUDE.md | Project root `CLAUDE.md` |
| SECRET.md | Project root `SECRET.md` |
| SPECIFICATION.md | `<app_folder>/context/specification/SPECIFICATION.md` |
| Source code | `<app_folder>/` (pom.xml, composer.json, package.json, etc.) |
| Application config | Stack-dependent (see detection) |

## Pre-Requisites

Before running this skill, the following must exist:
- **Source code** — The application must be implemented (not just context artifacts)
- **SPECIFICATION.md** — The technical specification must exist (generated by a `specgen-*` skill)
- **Externalized configuration** — The application config must use environment variables
  (e.g., `${ENV_VAR:default}` in Spring Boot, `env('VAR')` in Laravel, `process.env.VAR` in Node.js)

If any are missing, stop and inform the user.

## Workflow

### Phase 0: Validate Inputs

1. Resolve application folder from the provided name
2. Verify source code exists (at least one of: `pom.xml`, `composer.json`, `package.json`)
3. Verify `<app_folder>/context/specification/SPECIFICATION.md` exists
4. Read `CLAUDE.md` (already in context) for project-level information

### Phase 1: Detect Application Stack

#### Step 1 — Identify Stack

Check the application root for build files:

| File Found | Stack | Build Tool |
|---|---|---|
| `pom.xml` | `spring-boot` | Maven |
| `composer.json` | `laravel` | Composer |
| `package.json` (no `pom.xml` or `composer.json`) | `nodejs` | npm / pnpm |

If none found, stop with error: "Cannot determine application stack."

If multiple found (e.g., `pom.xml` + `package.json`), prioritize: `pom.xml` > `composer.json` > `package.json`
(the `package.json` is likely for frontend or E2E tests, not the main app).

#### Step 2 — Extract Stack-Specific Metadata

**If `spring-boot`:**
- Read `pom.xml`:
  - `<java.version>` → JDK version for base image
  - `<artifactId>` → image name
  - `<version>` → image tag
  - `frontend-maven-plugin` presence → has frontend build step
  - `jte-maven-plugin` presence → has JTE precompilation
  - `spring-boot-maven-plugin` → executable JAR packaging
- Read `src/main/resources/application.yml`:
  - Extract all `${ENV_VAR:default}` patterns via regex `\$\{([^:}]+)(?::([^}]*))?\}`
  - `server.port` default → exposed container port
- Read `.env` (if exists) → local dev values for reference

**If `laravel`:**
- Read `composer.json`:
  - `require.php` → PHP version for base image
  - `require.laravel/framework` → Laravel version
  - `name` → image name
- Read `.env.example` or scan `config/*.php`:
  - Extract all `env('VAR', 'default')` patterns via regex `env\('([^']+)'(?:,\s*'?([^')*]*)'?)?\)`
  - `APP_PORT` or default `8000` → exposed container port
- Check for `vite.config.js` or `webpack.mix.js` → has frontend build step
- Check for `package.json` in app root → Node.js needed for frontend build

**If `nodejs`:**
- Read `package.json`:
  - `engines.node` → Node.js version for base image
  - `name` → image name
  - `version` → image tag
  - `scripts.build` → build command (e.g., `tsc`, `tsup`, `vite build`, `next build`)
  - `scripts.start` → start command
- Scan source files or `.env` for `process.env.VAR` patterns
- Check for `tsconfig.json` → TypeScript build step
- Determine framework: Express, Fastify, NestJS, Next.js, etc.

#### Step 3 — Common Detection (All Stacks)

From `CLAUDE.md` → extract the "Depends on" list for the target application:

| Dependency Pattern | External Service |
|---|---|
| References MongoDB | MongoDB required |
| References MySQL / HC Database / SC Database | MySQL required |
| References PostgreSQL | PostgreSQL required |
| References Hub Single Sign On / Keycloak | Keycloak required |
| References RabbitMQ / Message Queue | RabbitMQ required |
| References Redis / Hub Cache | Redis required |
| References SMTP / Mailcatcher | SMTP service required |
| References Meilisearch / Hub Search Engine | Meilisearch required |

From `SPECIFICATION.md` → extract:
- Technology stack table (versions)
- Environment variable reference table (if present)

#### Step 4 — Classify Environment Variables

For each detected environment variable, classify as ConfigMap or Secret:

**Secret** (sensitive — stored in K8s Secret):
- Variable name contains `PASSWORD`, `SECRET`, `KEY`, `TOKEN`, `CREDENTIAL`
- Variable name contains `ADMIN_USERNAME` (admin-level credentials)
- Variable name is a database connection URI containing credentials

**ConfigMap** (non-sensitive — stored in K8s ConfigMap):
- Everything else: hostnames, ports, log levels, feature flags, queue names, CORS origins

#### Step 5 — Determine Health Check

| Stack | Default Health Endpoint | Condition |
|---|---|---|
| Spring Boot | `/actuator/health` | If `spring-boot-starter-actuator` in pom.xml |
| Spring Boot | `/` (HTTP 200/302 check) | If no actuator |
| Laravel | `/health` or `/` | Custom route or root |
| Node.js | `/health` or `/` | Custom route or root |

Check if a health endpoint is explicitly defined in the source code. If not, use the
default for the stack.

#### Step 6 — Present Detection Summary

Before generating, present findings to the user for confirmation:

```
Application Stack Detection:
- Stack:           Spring Boot 3.5.7 (Java 21)
- Artifact:        hub-middleware:1.0.3
- Frontend Build:  Yes (Vite via frontend-maven-plugin)
- JTE Precompile:  Yes (jte-maven-plugin)
- Exposed Port:    8080
- Health Check:    /actuator/health (actuator present)

External Services:
- MongoDB 7.0.6 (Hub Core Database)
- Keycloak 26.5.3 (Hub Single Sign On)
- RabbitMQ 3.11.9 (HC + SC Adapter Message Queues)
- Mailcatcher 0.8.1 (SMTP)

Environment Variables: 25 detected
- ConfigMap: 18 (hostnames, ports, log levels, queue names, flags)
- Secret: 7 (passwords, admin credentials)
```

If the user disagrees, allow overrides before proceeding.

### Phase 2: Generate or Update Dockerfile

1. Check if `<app_folder>/Dockerfile` already exists
2. **If it exists** (UPDATE mode):
   - Read the existing Dockerfile
   - Compare detected values against what the Dockerfile currently has:
     - Base image version (e.g., Java version changed in pom.xml)
     - Exposed port (e.g., server.port default changed)
     - Environment variable defaults (e.g., new env vars added)
     - Build commands (e.g., frontend build step added or removed)
   - Preserve any lines marked with `# CUSTOM:` comments — these are manual overrides
     by the user/DevOps that should not be replaced
   - Update only the parts that changed, keeping the overall structure intact
   - Log what was changed (e.g., "Updated Java version from 17 to 21",
     "Added MAIL_HOST env var")
3. **If it does not exist** (CREATE mode):
   - Select the Dockerfile pattern from `references/dockerfile-<stack>.md`
   - Substitute detected values (Java version, artifact name, port, etc.)
   - Write the Dockerfile to `<app_folder>/Dockerfile`

The Dockerfile MUST:
- Use a **multi-stage build** (build stage + runtime stage)
- Use **specific version tags** for base images (not `latest`)
- Run as a **non-root user** in the runtime stage
- **NOT** copy `.env`, `context/`, `e2e/`, or test files into the image
- Include a `HEALTHCHECK` instruction if a health endpoint is available
- Include comments explaining each significant instruction
- Include a `LABEL version="{version}"` instruction using the application version extracted
  from `pom.xml` (`<version>`), `composer.json` (`version`), or `package.json` (`version`)
- Include a build argument `ARG APP_VERSION={version}` that can be overridden at build time
  via `docker build --build-arg APP_VERSION=1.0.3`

The DEPLOYMENT.md **Docker Build** section must document tagging the image with the
application version:
```bash
docker build -t <image-name>:<version> .
docker tag <image-name>:<version> <image-name>:latest
```

The Kubernetes Deployment manifest in DEPLOYMENT.md must reference the versioned image tag
(e.g., `image: <image-name>:1.0.3`), NOT `latest`.

Read `references/dockerfile-spring-boot.md`, `references/dockerfile-laravel.md`, or
`references/dockerfile-nodejs.md` for the complete Dockerfile template per stack.

### Phase 3: Generate or Update DEPLOYMENT.md

1. Check if `<app_folder>/DEPLOYMENT.md` already exists
2. **If it exists** (UPDATE mode):
   - Read the existing DEPLOYMENT.md
   - Compare detected values against what the document currently has:
     - New or removed environment variables → update Section 3 (Environment Variables),
       Section 5.2 (ConfigMap), Section 5.3 (Secret), Section 6.4 (Ansible Variables)
     - New or removed dependencies → update Section 5.7 (Dependent Services),
       Section 6 (Ansible k8s-deps role)
     - Version changes (app version, dependency versions) → update all affected sections
     - Stack changes (unlikely but possible) → regenerate from scratch
   - Preserve any sections or blocks marked with `<!-- CUSTOM: do not overwrite -->` —
     these are manual additions by the user/DevOps
   - Log what was changed (e.g., "Added RABBITMQ_CR_SC_QUEUES to ConfigMap",
     "Updated MongoDB version from 6.0 to 7.0.6")
3. **If it does not exist** (CREATE mode):
   - Generate the full specification using detected values
   - Write to `<app_folder>/DEPLOYMENT.md`

Read the DEPLOYMENT.md structure below for the exact content of each section.

## DEPLOYMENT.md Structure

The generated DEPLOYMENT.md must follow this structure exactly. Every section must include
complete, copy-pasteable YAML/code samples — not just descriptions.

```markdown
# Deployment Specification — <Application Name>

> Generated: <date> | Stack: <stack> | Version: <app-version>
```

### Section 1: Overview

```markdown
## 1. Overview

**Application**: <name from CLAUDE.md>
**Description**: <description from CLAUDE.md>
**Stack**: <detected stack and version>
**Container Image**: <registry>/<image-name>:<tag>
**Exposed Port**: <detected port>
**Health Check**: <detected endpoint>
```

Include the technology stack table from SPECIFICATION.md.

### Section 2: Prerequisites

```markdown
## 2. Prerequisites

### Build Environment
- Docker 24+ (for multi-stage builds)
- Access to container registry

### Deployment Environment
- Kubernetes 1.28+
- kubectl configured with cluster access
- Ansible 2.15+ (on control node)
- Helm 3.x (optional, for chart-based deployment)

### External Services
<List each detected dependency with version and purpose>
```

### Section 3: Environment Variables

```markdown
## 3. Environment Variables

### ConfigMap Variables (Non-Sensitive)

| Variable | Description | Default | Production Example |
|----------|-------------|---------|-------------------|
| `SERVER_PORT` | Application HTTP port | `8080` | `8080` |
| `MONGODB_URI` | MongoDB connection URI | `mongodb://localhost:27017/db` | `mongodb://mongo-svc:27017/db` |
| ... | ... | ... | ... |

### Secret Variables (Sensitive)

| Variable | Description | Default | Notes |
|----------|-------------|---------|-------|
| `KEYCLOAK_ADMIN_PASSWORD` | Keycloak admin password | `(dev value)` | Store in K8s Secret or external vault |
| `RABBITMQ_PASSWORD` | RabbitMQ password | `guest` | Never use `guest` in production |
| ... | ... | ... | ... |
```

**Important**: The production example values must be realistic but not contain real
credentials. Use patterns like `<from-secret-manager>`, `mongodb://mongo-cluster:27017/db`,
`https://sso.example.com/realms/hub`.

### Section 4: Docker Build

```markdown
## 4. Docker Build

### Dockerfile Location
`<app_folder>/Dockerfile`

### Build Commands

#### Local Build
\```bash
docker build -t <image-name>:<tag> .
\```

#### Build with Build Args (if applicable)
\```bash
docker build \
  --build-arg JAR_FILE=target/<artifact>.jar \
  -t <image-name>:<tag> .
\```

### Image Tagging Strategy

| Tag Pattern | Usage | Example |
|-------------|-------|---------|
| `<version>` | Release versions | `hub-middleware:1.0.3` |
| `<version>-<git-sha>` | Traceability | `hub-middleware:1.0.3-a1b2c3d` |
| `latest` | Latest stable release | `hub-middleware:latest` |

### Push to Registry
\```bash
docker tag <image-name>:<tag> <registry>/<image-name>:<tag>
docker push <registry>/<image-name>:<tag>
\```
```

### Section 5: Kubernetes Manifests

Each subsection must include a complete, ready-to-use YAML manifest.

#### 5.1 Namespace

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: <namespace>
  labels:
    app.kubernetes.io/part-of: <project-name>
```

#### 5.2 ConfigMap

Generated from the ConfigMap variables in Section 3. All non-sensitive environment
variables go here.

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: <app-name>-config
  namespace: <namespace>
data:
  SERVER_PORT: "8080"
  MONGODB_URI: "mongodb://mongo-svc:27017/<db-name>"
  # ... all ConfigMap variables
```

#### 5.3 Secret

Generated from the Secret variables in Section 3. All sensitive environment variables
go here. Values are base64-encoded in the manifest.

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: <app-name>-secret
  namespace: <namespace>
type: Opaque
data:
  KEYCLOAK_ADMIN_PASSWORD: <base64-encoded>
  RABBITMQ_PASSWORD: <base64-encoded>
  # ... all Secret variables
```

Include a note about using external secret management (e.g., HashiCorp Vault,
AWS Secrets Manager, Azure Key Vault) in production instead of inline K8s Secrets.

#### 5.4 Deployment

The application pod specification. Must include:
- Container image reference
- Resource requests and limits (sensible defaults per stack)
- Liveness and readiness probes using the detected health endpoint
- Environment variables injected from ConfigMap and Secret
- Security context (non-root, read-only root filesystem where possible)

**Resource defaults by stack:**

| Stack | CPU Request | CPU Limit | Memory Request | Memory Limit |
|---|---|---|---|---|
| Spring Boot | 250m | 1000m | 512Mi | 1024Mi |
| Laravel | 100m | 500m | 128Mi | 512Mi |
| Node.js | 100m | 500m | 128Mi | 512Mi |

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: <app-name>
  namespace: <namespace>
spec:
  replicas: 2
  selector:
    matchLabels:
      app: <app-name>
  template:
    metadata:
      labels:
        app: <app-name>
    spec:
      containers:
        - name: <app-name>
          image: <registry>/<image-name>:<tag>
          ports:
            - containerPort: <port>
          envFrom:
            - configMapRef:
                name: <app-name>-config
            - secretRef:
                name: <app-name>-secret
          resources:
            requests:
              cpu: <cpu-request>
              memory: <mem-request>
            limits:
              cpu: <cpu-limit>
              memory: <mem-limit>
          livenessProbe:
            httpGet:
              path: <health-endpoint>
              port: <port>
            initialDelaySeconds: <stack-dependent>
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: <health-endpoint>
              port: <port>
            initialDelaySeconds: <stack-dependent>
            periodSeconds: 10
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
```

**Probe initial delay by stack:**

| Stack | Liveness Delay | Readiness Delay | Reason |
|---|---|---|---|
| Spring Boot | 60s | 30s | JVM warmup + Spring context initialization |
| Laravel | 15s | 10s | PHP-FPM starts fast |
| Node.js | 15s | 10s | Node starts fast |

#### 5.5 Service

```yaml
apiVersion: v1
kind: Service
metadata:
  name: <app-name>-svc
  namespace: <namespace>
spec:
  type: ClusterIP
  selector:
    app: <app-name>
  ports:
    - port: <port>
      targetPort: <port>
      protocol: TCP
```

Include a note: use `type: NodePort` or `type: LoadBalancer` if external access is
needed without an Ingress controller.

#### 5.6 Ingress (Optional)

Include if the application has CORS origins configured (indicating external access)
or if the application serves a web UI.

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: <app-name>-ingress
  namespace: <namespace>
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: nginx
  rules:
    - host: <app-domain>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: <app-name>-svc
                port:
                  number: <port>
  tls:
    - hosts:
        - <app-domain>
      secretName: <app-name>-tls
```

#### 5.7 Dependent Services

For EACH external service detected in Phase 1 Step 3, provide TWO options:

**Option A — Deploy in Kubernetes** (for dev/staging or self-hosted production):
Complete StatefulSet/Deployment + Service + PersistentVolumeClaim manifest.

**Option B — Reference External Service** (for managed/existing production instances):
ExternalName Service or static Endpoints manifest.

**Per-service patterns:**

| Service | Option A | Option B |
|---|---|---|
| MongoDB | StatefulSet + PVC + headless Service | ExternalName to managed MongoDB |
| MySQL | StatefulSet + PVC + headless Service | ExternalName to managed MySQL |
| PostgreSQL | StatefulSet + PVC + headless Service | ExternalName to managed PostgreSQL |
| Keycloak | Deployment + Service + Ingress | ExternalName to existing Keycloak |
| RabbitMQ | StatefulSet + PVC + Service | ExternalName to managed RabbitMQ |
| Redis | StatefulSet + PVC + Service | ExternalName to managed Redis |
| Meilisearch | Deployment + PVC + Service | ExternalName to managed instance |
| Mailcatcher | Deployment + Service (dev/staging only) | Omit in production (use real SMTP) |

Use the versions from CLAUDE.md for container image tags.

### Section 6: Ansible Playbook

#### 6.1 Inventory Structure

```markdown
### 6.1 Inventory Structure

\```
ansible/
├── inventory/
│   ├── dev/
│   │   ├── hosts.yml
│   │   └── group_vars/
│   │       └── all.yml          ← dev-specific variables
│   ├── staging/
│   │   ├── hosts.yml
│   │   └── group_vars/
│   │       └── all.yml          ← staging-specific variables
│   └── prod/
│       ├── hosts.yml
│       └── group_vars/
│           └── all.yml          ← prod-specific variables
├── roles/
│   ├── docker-build/
│   │   └── tasks/main.yml       ← Build + push container image
│   ├── k8s-namespace/
│   │   └── tasks/main.yml       ← Create namespace
│   ├── k8s-config/
│   │   └── tasks/main.yml       ← Apply ConfigMap + Secret
│   ├── k8s-deploy/
│   │   └── tasks/main.yml       ← Apply Deployment + Service + Ingress
│   ├── k8s-deps/
│   │   └── tasks/main.yml       ← Deploy dependent services (Option A)
│   └── k8s-verify/
│       └── tasks/main.yml       ← Health check after deployment
├── deploy.yml                    ← Main playbook
└── rollback.yml                  ← Rollback playbook
\```
```

#### 6.2 Roles

For each role, provide complete `tasks/main.yml` content.

**docker-build** — Build and push the container image:
- Checkout source code (or assume local)
- Run `docker build` with the Dockerfile
- Tag the image
- Push to registry

**k8s-namespace** — Create the namespace:
- Apply namespace manifest
- Idempotent (no error if exists)

**k8s-config** — Apply ConfigMap and Secret:
- Template the ConfigMap from inventory variables
- Template the Secret from vault/encrypted variables
- Apply via `kubernetes.core.k8s` module

**k8s-deploy** — Deploy the application:
- Apply Deployment manifest
- Apply Service manifest
- Apply Ingress manifest (if applicable)
- Wait for rollout to complete

**k8s-deps** — Deploy dependent services (optional):
- For each dependency, apply Option A manifests
- Idempotent (skip if already running)

**k8s-verify** — Post-deployment verification:
- Wait for pods to be Ready
- Hit health endpoint from within cluster
- Report status

#### 6.3 Playbook

Complete `deploy.yml` that ties all roles together:

```yaml
---
- name: Deploy <Application Name>
  hosts: k8s_control_plane
  vars:
    app_name: <app-name>
    app_version: <version>
    image_name: <registry>/<image-name>
    image_tag: "{{ app_version }}"
    namespace: <namespace>
  roles:
    - docker-build
    - k8s-namespace
    - k8s-deps        # Optional: deploy dependent services
    - k8s-config
    - k8s-deploy
    - k8s-verify
```

#### 6.4 Variables

Complete `group_vars/all.yml` template with all environment variables from Section 3,
organized by service and marked with `# vault-encrypted` for sensitive values.

### Section 7: Deployment Procedures

```markdown
## 7. Deployment Procedures

### 7.1 First-Time Setup
Step-by-step instructions for initial deployment from scratch.

### 7.2 Routine Deployment (Version Upgrade)
Steps to deploy a new version (build, push, rolling update).

### 7.3 Rollback
Steps to rollback to a previous version using:
- `kubectl rollout undo`
- Ansible rollback playbook

### 7.4 Scaling
- Horizontal: `kubectl scale deployment <app> --replicas=N`
- Vertical: Update resource limits in Deployment manifest
- HPA (Horizontal Pod Autoscaler) configuration sample
```

### Section 8: Health Checks & Monitoring

```markdown
## 8. Health Checks & Monitoring

### Probes
- Liveness: <endpoint> (restarts pod if unhealthy)
- Readiness: <endpoint> (removes from service if not ready)

### Logging
- Application logs: stdout/stderr (collected by K8s)
- Log format: <from application.yml logging config>
- Recommended: Fluentd/Fluent Bit sidecar or DaemonSet for log aggregation

### Monitoring (Recommendations)
- Prometheus metrics endpoint (if actuator/metrics available)
- Grafana dashboard for application metrics
- AlertManager rules for pod restarts, high error rates
```

### Section 9: Security Considerations

```markdown
## 9. Security Considerations

### Container Security
- Non-root user (UID 1000) in Dockerfile
- Read-only root filesystem where possible
- No unnecessary packages in runtime image
- Image vulnerability scanning (Trivy, Snyk)

### Secret Management
- K8s Secrets for basic deployments
- External vault (HashiCorp Vault, AWS Secrets Manager) for production
- Never store secrets in ConfigMaps, environment files, or source control

### Network Policies
- Restrict pod-to-pod communication to only necessary paths
- Sample NetworkPolicy manifests for:
  - App → Database
  - App → Message Broker
  - App → Auth Provider
  - Ingress → App

### Image Registry
- Use a private registry
- Enable image pull secrets in K8s
- Sign images with cosign/notation (recommended)
```

## Constraints

These constraints are non-negotiable:

1. **Stack-agnostic output** — The skill must work for Spring Boot, Laravel, and Node.js
   applications. All stack-specific logic is gated behind the detection in Phase 1.

2. **Real values from context** — Every manifest, playbook, and configuration sample must
   use the actual application name, port, environment variables, and dependency versions
   from the project's context files. No placeholder `<TODO>` values except for
   legitimately unknown production values (registry URL, domain name, credentials).

3. **Two options for dependencies** — Every dependent service must have both Option A
   (deploy in K8s) and Option B (reference external). The user or DevOps team decides
   which to use per environment.

4. **ConfigMap/Secret separation** — Sensitive values must NEVER appear in ConfigMaps.
   The classification logic in Phase 1 Step 4 must be applied consistently.

5. **Non-root containers** — All Dockerfiles must run the application as a non-root user.

6. **Multi-stage builds** — All Dockerfiles must use multi-stage builds to minimize the
   runtime image size. Build tools (Maven, Composer, npm dev dependencies) must NOT be
   in the final image.

7. **No `.env` in containers** — The `.env` file is for local development only. Container
   environments receive configuration via K8s ConfigMap + Secret injection.

8. **Copy-pasteable samples** — Every YAML block in DEPLOYMENT.md must be a complete,
   valid manifest that can be applied directly with `kubectl apply -f`. No fragments
   stitched with `# ...` gaps.

9. **Idempotent — supports both create and update** — If Dockerfile or DEPLOYMENT.md
   already exist, read them first, detect what changed (new env vars, version bumps,
   added/removed dependencies), and update only the affected parts. Preserve any manual
   customizations marked with `# CUSTOM:` (Dockerfile) or `<!-- CUSTOM: do not overwrite -->`
   (DEPLOYMENT.md). If neither file exists, create from scratch. Always log what was
   created or changed so the user can review.

10. **Single-pass execution** — This skill completes in one pass (two files generated).
    No Ralph Loop needed.

11. **Production-safe defaults** — Dockerfile and K8s manifests must default to
    production-safe values. Dev-specific settings (JTE dev mode, DevTools, debug logging)
    must be OFF by default in the container, overridden only via explicit environment
    variables.
